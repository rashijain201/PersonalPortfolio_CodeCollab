const largerSectionHeading = ({ text }) => (
    <h1
        className={`text-6xl font-bold text-purple-700 dark:text-purple-400 text-center`}
    >
        {text}
    </h1>
);

export default largerSectionHeading;
